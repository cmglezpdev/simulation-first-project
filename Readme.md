## Requirements
  - pipenv
  ```bash
    pip install pipenv

    ```
## Install All dependencies
   ```bash
    pipenv install
```
### In Order to run the project do:
 ```bash
streamlit run gui.py

```
